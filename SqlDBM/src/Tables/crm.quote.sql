-- ************************* SqlDBM: Databricks *************************
-- *** Generated by SqlDBM: Databricks 32 by eric.ruiz+yew@sqldbm.com ***

-- ************************************** crm.quote
CREATE TABLE crm.quote
(
 QuoteId       varchar(18) NOT NULL COMMENT 'Unique identifier for each quote, serving as the primary key.' CONSTRAINT pk_quote PRIMARY KEY,
 OpportunityId varchar(18) COMMENT 'References the opportunity associated with the quote.' CONSTRAINT opportunityid REFERENCES crm.opportunity,
 Status        string COMMENT 'Tracks the current status of the quote (e.g., Draft, Approved, Rejected).',
 TotalPrice    decimal(18,2) COMMENT 'The total monetary value of the quote.',
 CreatedDate   date COMMENT 'The date when the quote record was created.'
)
USING DELTA
 COMMENT 'Manages customer-facing pricing proposals, linking them to specific opportunities.'
 TBLPROPERTIES (delta.minWriterVersion = 7, delta.enableDeletionVectors = true, delta.minReaderVersion = 3, delta.checkpoint.writeStatsAsStruct = true, delta.checkpoint.writeStatsAsJson = false, delta.feature.deletionVectors = 'supported', delta.feature.invariants = 'supported');
