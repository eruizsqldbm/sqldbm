-- ************************** SqlDBM: Databricks *************************
-- * Generated by SqlDBM: Databricks Demo 65 by eric.ruiz+yew@sqldbm.com *

-- ************************************** CRM.SILVER_USER
CREATE TABLE CRM.SILVER_USER
(
 USER_ID      varchar(18) NOT NULL COMMENT 'Unique identifier for each user in the CRM. This ID is used as the primary key for linking user records throughout the system.' CONSTRAINT PK_USER PRIMARY KEY,
 USERNAME     string NOT NULL COMMENT 'Specifies the unique username for the user, used for login and identification.',
 EMAIL        string COMMENT 'Stores the email address of the user. This is used for system notifications and communication.',
 IS_ACTIVE    boolean COMMENT 'Indicates whether the users account is active. Active users can log in and access the CRM, while inactive users cannot.',
 PROFILE_ID   varchar(18) COMMENT 'References the Profile table to define permissions and access for this user.',
 CREATED_DATE date COMMENT 'The date and time when this user record was created in the system.',
 Last_Updated timestamp NOT NULL
)
USING DELTA
 COMMENT 'Contains information about CRM system users, including their login credentials, roles, and activity statuses.'
 TBLPROPERTIES (delta.minWriterVersion = 7, delta.enableDeletionVectors = true, delta.minReaderVersion = 3, delta.checkpoint.writeStatsAsStruct = true, delta.checkpoint.writeStatsAsJson = false, delta.feature.deletionVectors = 'supported', delta.feature.invariants = 'supported');
