-- *************************** SqlDBM: Snowflake **************************
-- * Generated by SqlDBM: Sample - Data Model by eric.ruiz+yew@sqldbm.com *

-- ************************************** CRM.CONTRACT
CREATE TABLE IF NOT EXISTS CRM.CONTRACT
(
 CONTRACT_ID       char(18) NOT NULL COMMENT 'Unique identifier for each contract, serving as the primary key.',
 ACC_ID            char(18) COMMENT 'References the account associated with this contract.',
 START_DATE        date COMMENT 'Specifies the start date of the contract.',
 END_DATE          date COMMENT 'Specifies the end date or expiration of the contract.',
 STATUS            varchar(20) COMMENT 'Tracks the current status of the contract (e.g., Active, Expired).',
 CREATED_DATE      date COMMENT 'The date when the contract record was created in the CRM.',
 LOYALTYPROGRAMKEY number(38,0) NOT NULL COMMENT 'The ''LoyaltyProgramKey'' column contains data related to ''loyaltyprogramkey''.',
 ADDRESS_1         string NOT NULL,

 CONSTRAINT PK_CONTRACT PRIMARY KEY ( CONTRACT_ID ),
 CONSTRAINT ACC_ID FOREIGN KEY ( ACC_ID ) REFERENCES CRM.ACCOUNT ( ACC_ID ),
 CONSTRAINT FK_77 FOREIGN KEY ( LOYALTYPROGRAMKEY ) REFERENCES SUPPLYCHAIN.CUSTOMERLOYALTYPROGRAMDIM ( LOYALTYPROGRAMKEY )
)
COMMENT = 'Manages customer agreements, tracking contract duration, status, and associated accounts.';
