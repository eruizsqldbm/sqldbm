-- ************************* SqlDBM: Databricks *************************
-- *** Generated by SqlDBM: Databricks 32 by eric.ruiz+yew@sqldbm.com ***

-- ************************************** crm.pricebookentry
CREATE TABLE crm.pricebookentry
(
 EntryId     varchar(18) NOT NULL COMMENT 'Unique identifier for each pricebook entry, serving as the primary key.' CONSTRAINT pk_pricebookentry PRIMARY KEY,
 PricebookId varchar(18) COMMENT 'References the pricebook in which this product-price association is defined.' CONSTRAINT pricebookid REFERENCES crm.pricebook,
 ProductId   varchar(18) COMMENT 'References the product associated with this pricebook entry.' CONSTRAINT productid REFERENCES crm.product,
 UnitPrice   decimal(18,2) COMMENT 'Specifies the price of one unit of the product in the context of this pricebook.',
 IsActive    boolean COMMENT 'Indicates whether the pricebook entry is active and available for use in transactions.',
 CreatedDate date COMMENT 'The date when the pricebook entry was created in the CRM.'
)
USING DELTA
 COMMENT 'Connects products to specific pricebooks, defining pricing for various sales and marketing contexts.'
 TBLPROPERTIES (delta.minWriterVersion = 7, delta.enableDeletionVectors = true, delta.minReaderVersion = 3, delta.checkpoint.writeStatsAsStruct = true, delta.checkpoint.writeStatsAsJson = false, delta.feature.deletionVectors = 'supported', delta.feature.invariants = 'supported');
