-- ************************** SqlDBM: Databricks *************************
-- * Generated by SqlDBM: Databricks Demo 67 by eric.ruiz+yew@sqldbm.com *

-- ************************************** POS.ORDER_ITEM_T
CREATE TABLE POS.ORDER_ITEM_T
(
 ORDER_ITEM_ID decimal(10,0) NOT NULL COMMENT 'A unique identifier for each order item' CONSTRAINT PK_ORDER_ITEM PRIMARY KEY,
 ORDER_ID      decimal(10,0) COMMENT 'The order to which the item belongs, linked to the Order table.' CONSTRAINT ORDER_ID REFERENCES POS.ORDER_T,
 MENU_ITEM_ID  decimal(10,0) COMMENT 'The menu item ordered, linked to the MenuItem table.' CONSTRAINT MENU_ITEM_ID_746 REFERENCES POS.MENU_ITEM_T,
 QUANTITY      decimal(10,0) NOT NULL COMMENT 'The quantity of the menu item ordered.',
 UNIT_PRICE    decimal(10, 2) NOT NULL COMMENT 'The price per unit of the menu item.',
 LastUpdated   timestamp NOT NULL
)
 COMMENT 'Table stores the details of individual items within an order.';
