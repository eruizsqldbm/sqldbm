-- ************************** SqlDBM: BigQuery **************************
-- ***** Generated by SqlDBM: GBQ Demo1 by eric.ruiz+yew@sqldbm.com *****

-- ************************************** POS.ORDER_DISCOUNT
CREATE TABLE POS.ORDER_DISCOUNT
(
ORDER_DISCOUNT_ID numeric NOT NULL PRIMARY KEY NOT ENFORCED OPTIONS(description = 'A unique identifier for the order discount.'),
ORDER_ID          numeric OPTIONS(description = 'The order to which the discount is applied, linked to the Order table.'),
DISCOUNT_ID       numeric OPTIONS(description = 'The discount applied to the order, linked to the Discount table'),
 CONSTRAINT ORDER_ID FOREIGN KEY (ORDER_ID) REFERENCES POS.ORDER (ORDER_ID) NOT ENFORCED,
 CONSTRAINT DISCOUNT_ID FOREIGN KEY (DISCOUNT_ID) REFERENCES POS.DISCOUNT (DISCOUNT_ID) NOT ENFORCED
)
OPTIONS(description = 'Table stores discounts applied to individual orders.',
friendly_name = 'OrderDiscount');
