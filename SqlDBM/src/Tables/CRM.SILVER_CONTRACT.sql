-- ************************** SqlDBM: Databricks *************************
-- * Generated by SqlDBM: Databricks Demo 65 by eric.ruiz+yew@sqldbm.com *

-- ************************************** CRM.SILVER_CONTRACT
CREATE TABLE CRM.SILVER_CONTRACT
(
 CONTRACT_ID  varchar(18) NOT NULL COMMENT 'Unique identifier for each contract, serving as the primary key.' CONSTRAINT PK_CONTRACT PRIMARY KEY,
 ACCOUNT_ID   varchar(18) COMMENT 'References the account associated with this contract.' CONSTRAINT ACCOUNTID_1 REFERENCES CRM.SILVER_ACCOUNT,
 START_DATE   date COMMENT 'Specifies the start date of the contract.',
 END_DATE     date COMMENT 'Specifies the end date or expiration of the contract.',
 STATUS       string COMMENT 'Tracks the current status of the contract (e.g., Active, Expired).',
 CREATED_DATE date COMMENT 'The date when the contract record was created in the CRM.',
 Last_Updated timestamp NOT NULL
)
USING DELTA
 COMMENT 'Manages customer agreements, tracking contract duration, status, and associated accounts.'
 TBLPROPERTIES (delta.minWriterVersion = 7, delta.enableDeletionVectors = true, delta.minReaderVersion = 3, delta.checkpoint.writeStatsAsStruct = true, delta.checkpoint.writeStatsAsJson = false, delta.feature.deletionVectors = 'supported', delta.feature.invariants = 'supported');
